# Shell-скрипты обязаны быть с LF: с CRLF bash на Ubuntu падает
# с «$'\r': command not found» и деплой не проходит.
*.sh text eol=lf
# Батник наоборот — cmd любит CRLF
*.bat text eol=crlf
* text=auto
